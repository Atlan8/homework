1.DTD文档声明
-告诉浏览器，当前网页是用哪一个版本的HTML规范来编写的
 -每一个不同版本的规范都有不同的DTD文档声明
 -HTML5的DTD文档声明格式：<!DOCTYPE html>

2.注意点：
-任何一个标准的HTML网页，第一行一定是DTD文档声明
-DTD文档声明不区分大小写
-DTD文档声明不是一个标签
-每一个浏览器都有一套自己的识别方式，即使HTML文档中没有DTD声明
浏览器也能正确渲染出当前网页，但是W3C规定了HTML文档的第一行
必须是DTD声明，所以为了遵守规定，我们在编写网页的时候一定要加上
DTD文档声明！！